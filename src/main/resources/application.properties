# Nombre de la aplicación
spring.application.name=educfusionbd

# Configuración de la fuente de datos
spring.datasource.url=jdbc:mysql://localhost:3306/educfusionbd
spring.datasource.username=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Configuración de los logs de Hibernate
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicTypeRegistry=TRACE

# Otras configuraciones opcionales
server.port=8080
